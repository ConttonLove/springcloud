server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native  # 本地文件
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config # 本地文件访问路径